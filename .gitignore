# Logs
logs
*.log
npm-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store

# Agent
.agent/
.claude/
# As skills do projeto SAO versionadas: elas guardam o metodo, nao a
# configuracao da maquina. `/disputa` e o unico lugar onde esta escrito por que
# o dossie de contestacao e daquele jeito.
!.claude/skills/
!.claude/skills/**
.vercel
scratch/
!scratch/testar-rota.mjs

# Backup com PII do projeto angelical — nunca comitar
backup-angelical/

.vercel
public/exemplos/
materiais/

# estado local do supabase cli (tem pooler-url com string de conexao)
supabase/.temp/

# Os dossiês de contestação SÃO versionados, apesar de nascerem de um script.
# Cada um é o registro do que a gente afirmou a um adquirente, e disputa volta
# a ser pedida meses depois.
!docs/disputas/
